{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "forge_lineage/schemas/payloads/fake_consumer_use.v1",
  "title": "FakeConsumerUsePayload.v1",
  "type": "object",
  "required": ["schema_version", "consumer_id", "consumed_at"],
  "additionalProperties": false,
  "properties": {
    "schema_version": {"type": "string", "const": "fake_consumer_use.v1"},
    "consumer_id": {"type": "string", "minLength": 1, "maxLength": 256},
    "consumed_at": {"type": "string", "format": "date-time"},
    "decision": {"type": "string", "maxLength": 256}
  }
}
